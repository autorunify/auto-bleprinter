import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'auto.bleprinter.example',
  appName: 'auto-bleprinter-example',
  webDir: 'dist'
};

export default config;
